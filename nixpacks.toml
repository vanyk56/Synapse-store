# Nixpacks configuration to override phases on Railway

[phases.install]
cmds = [
  "pnpm install --no-frozen-lockfile",
  "pnpm --filter @workspace/api-server exec playwright install chromium --with-deps"
]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm run start"
